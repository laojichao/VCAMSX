package com.wangyiheng.vcamsx.camerahook

/**
 * Camera2 API 相关的 Hook 处理类。
 *
 * 负责处理 Android Camera2（[android.hardware.camera2]）API 的拦截与替换逻辑，
 * 包括 CameraManager.openCamera、CaptureSession 创建等方法的 Hook。
 */
class CameraTwo {
}